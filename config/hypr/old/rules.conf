# Dialogs
windowrule {
    name = dialog-open-file
    match:title = ^Open File.*

    float = yes
    center = yes
}

windowrule {
    name = dialog-select-file
    match:title = ^Select a File.*

    float = yes
    center = yes
}

windowrule {
    name = dialog-choose-wallpaper
    match:title = ^Choose wallpaper.*

    float = yes
    center = yes
}

windowrule {
    name = dialog-open-folder
    match:title = ^Open Folder.*

    float = yes
    center = yes
}

windowrule {
    name = dialog-save-as
    match:title = ^Save As.*

    float = yes
    center = yes
}

windowrule {
    name = dialog-library
    match:title = ^Library.*

    float = yes
    center = yes
}

# # Ignore maximize requests from apps. You'll probably like this.
# windowrulev2 = suppressevent maximize, class:.*
#
# # Fix some dragging issues with XWayland
# windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

############################
# Floating applications
############################

# btop (Alacritty)
windowrule {
    name = btop
    match:class = ^Alacritty$
    match:title = ^btop$

    float = yes
    size = (monitor_w*0.80) (monitor_h*0.80)
    center = yes
}

# calc (Alacritty)
windowrule {
    name = calc
    match:class = ^Alacritty$
    match:title = ^calc$

    float = yes
    size = (monitor_w*0.45) (monitor_h*0.45)
    move = (monitor_w*0.53) (monitor_h*0.10)
}

# Calcure (calendar)
windowrule {
    name = calcure
    match:class = ^Alacritty$
    match:title = ^calcure$

    float = yes
    size = (monitor_w*0.70) (monitor_h*0.70)
    center = yes
}

############################
# Utilities
############################

# Qalculate
windowrule {
    name = qalculate
    match:class = ^io.github.Qalculate.qalculate-qt$

    float = yes
    size = (monitor_w*0.55) (monitor_h*0.60)
}

# Pavucontrol
windowrule {
    name = pavucontrol
    match:class = ^org.pulseaudio.pavucontrol$

    float = yes
    size = (monitor_w*0.36) (monitor_h*0.75)
    move = (monitor_w*0.57) (monitor_h*0.06)
}

# Blueman
windowrule {
    name = blueman
    match:class = ^blueman-manager$

    float = yes
    size = (monitor_w*0.40) (monitor_h*0.75)
    move = (monitor_w*0.58) (monitor_h*0.06)
}

############################
# Blender
############################

# Blender Preferences
windowrule {
    name = blender-preferences
    match:class = ^blender$
    match:title = ^Blender Preferences$

    float = yes
    size = (monitor_w*0.65) (monitor_h*0.75)
    center = yes
}

############################
# Picture-in-Picture
############################

windowrule {
    name = picture-in-picture
    match:title = ^Picture-in-Picture$

    float = yes
    size = (monitor_w*0.50) (monitor_h*0.50)
}

############################
# Firefox
############################

# Firefox Library
windowrule {
    name = firefox-library
    match:class = ^firefox$
    match:title = ^Library$

    float = yes
    size = (monitor_w*0.65) (monitor_h*0.75)
    center = yes
}

# Firefox – Hermesoft
windowrule {
    name = firefox-hermesoft
    match:class = ^firefox$
    match:title = ^Vortal Hermesoft.*

    float = yes
    size = (monitor_w*0.75) (monitor_h*0.88)
    center = yes
}

# Firefox – Academusoft
windowrule {
    name = firefox-academusoft
    match:class = ^firefox$
    match:title = ^Academic - Academusoft.*

    float = yes
    size = (monitor_w*0.75) (monitor_h*0.88)
    center = yes
}

############################
# Prism Launcher
############################

# Console window
windowrule {
    name = prism-console
    match:class = ^org.prismlauncher.PrismLauncher$
    match:title = ^Console window for 1.20.6 — Prism Launcher 8.3$

    float = yes
    size = (monitor_w*0.80) (monitor_h*0.80)
    center = yes
}

############################
# GitHub Desktop
############################

windowrule {
    name = github-desktop
    match:class = ^GitHub Desktop$

    float = yes
    size = (monitor_w*1.00) (monitor_h*1.00)
    center = yes
}

windowrule {
    name = vivado
    match:class = ^Vivado.*$

    float = yes
    center = yes
}

# ############################
# # Layer rules
# ############################
#
# layerrule = no_anim, match:class hyprpicker
# layerrule = no_anim, match:class selection
#```
